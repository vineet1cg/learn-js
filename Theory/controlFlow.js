const userLoggedIn = true;

if (userLoggedIn) {
  // if the boolean value inside the variable is true then only this block of code will work else it would not execute
  // there are relational operators such as < , > , <= , >= , == , != , === for strict equality
  // you now must know the meaning of this
}
